import { IAzureDrawingLayerStatefulProviderProps, LayerType } from '../types';
export declare const useAzureMapDrawingLayer: ({ options, type, events, lifecycleEvents }: IAzureDrawingLayerStatefulProviderProps) => {
    layerRef: LayerType | null;
};
